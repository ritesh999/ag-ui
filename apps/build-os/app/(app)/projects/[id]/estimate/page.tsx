import { Card } from "@/components/ui";

// The Pricing Schedule + pricing engine and Project Resources sub-tabs
// (spec 2.3) are step 5. This tab exists now (project detail's 3-tab
// shell is step 4's own deliverable) but its content isn't built yet.
export default function EstimatePage() {
  return (
    <Card className="py-12 text-center text-sm text-mid-gray">
      The Pricing Schedule and pricing engine land in step 5.
    </Card>
  );
}

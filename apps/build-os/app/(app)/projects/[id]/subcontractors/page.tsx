import { Card } from "@/components/ui";

// The Work Breakdown Structure / procurement UI (spec 2.2) is step 6 of
// the build order. This tab exists now (project detail's 3-tab shell is
// step 4's own deliverable) but its content isn't built yet.
export default function SubcontractorsPage() {
  return (
    <Card className="py-12 text-center text-sm text-mid-gray">
      Work Breakdown Structure & procurement packages land in step 6.
    </Card>
  );
}
